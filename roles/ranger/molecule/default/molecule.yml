---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: instance
    image: ubuntu:18.04
#    env:
#      http_proxy: ${http_proxy}
#      https_proxy: ${https_proxy}
#      no_proxy: ${no_proxy}
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8
